using UnityEngine;
using System.Collections;

public class TestParticleSpeed : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	//private float zoom;
	//void Update () {
		//zoom=Mathf.Lerp(zoom,AnalyzeMusic.rmsValue*15,1*Time.deltaTime);
	//}
	
/*	void LateUpdate() {
		Particle[] particles = particleEmitter.particles;
		int i = 0;
		while (i < particles.Length) {
			//float yPosition = Mathf.Sin(Time.time) * Time.deltaTime;
			//particles[i].position = new Vector3(particles[i].position.x, -4+zoom, particles[i].position.z);
			//particles[i].color = Color.red;
			//particles[i].size = Mathf.Sin(Time.time) * 0.2F;
			//particles[i].velocity=new Vector3(0, 0, -1f-AnalyzeFrequencies.tempo);
			i++;
		}
		particleEmitter.particles = particles;
	}*/
}
